const mathFunctions = require("./functions");

//window refers to the browser window; Node.js does not hace a browser so it doesnt work
console.log("help!!!");

console.log(mathFunctions.quadraticFormula(1, -3, 2)); // [2, 1]
